
:::writing{variant="standard" id="58472"}
name: My First Workflow

on:
  push:
    branches:
      - main

jobs:
  first-job:
    runs-on: ubuntu-latest

    steps:
      - name: First Step
        run: echo "Hello DevOps 🚀"
:::

---

# 🧠 Main Problems

❌ Missing spaces/indentation  
❌ Extra triple backticks:

```text id="p3n8xz"
